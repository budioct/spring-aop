package com.tutorial.aop.pengenalan;

public class PengenalanAOP {

    /**
     * Pengenalan AOP (intinya membantu penulisan log satu kali saja. yang bisa melintasi antar method dan object)
     * ● AOP atau singkatan dari Aspect Oriented Programming
     * ● AOP melengkapi OOP (Object Oriented Programming) dalam membuat kode program
     * ● Saat kita menggunakan OOP,
     *   - inti dari modularity kode adalah class,
     *   sedangkan di AOP, inti dari
     *   - modularity kode adalah aspect
     * ● Aspect memungkinkan modularity dari concerns (perhatian), yang bisa melintasi berbagai jenis tipe data dan object
     *
     * Spring AOP
     * ● Spring memiliki dua fitur AOP, yang pertama implementasi sendiri menggunaka Spring AOP, yang
     *   kedua menggunakan library AOP yang bernama AspectJ
     * ● Pada kelas ini, kita akan membahas yang menggunakan library AspectJ, karena banyak digunakan
     *   di Spring, salah satunya untuk fitur Declarative Transaction Management
     *
     *   Membingungkan
     * ● AOP biasanya akan sangat membingungkan ketika pertama kali mengenal istilahnya
     * ● Tidak perlu khawatir, karena itu memang sudah biasa untuk programmer yang biasa menggunakan OOP
     * ● Namun nanti ketika kita sudah lihat contoh-contoh penggunaan AOP, maka kita bisa dengan
     *   mudah mengerti tentang AOP
     */

}
